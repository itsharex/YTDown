# URL履歴機能 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** プレイリスト機能を削除し、動画・画像それぞれのURL入力欄に直近10件の履歴ドロップダウンを追加する

**Architecture:** SQLite に `url_history` テーブルを追加し、Rust コマンド経由で UPSERT + 自動プルーニング。フロントエンドは共通の `UrlHistoryDropdown.vue` コンポーネントを2箇所に配置。

**Tech Stack:** Tauri v2 (Rust/rusqlite), Vue 3 (Composition API), TypeScript, Tailwind CSS

**Spec:** `docs/superpowers/specs/2026-03-25-url-history-design.md`

**注意:** `fetch_playlist_items`（download.rs）はYouTubeプレイリストをyt-dlpで取得するコアダウンロード機能であり、削除対象外。削除するのはアプリ独自のプレイリスト管理機能（playlists.rs, url_lists.rs）のみ。

---

## ファイル構成

### 削除するファイル
- `src/components/playlist/PlaylistList.vue`
- `src/components/playlist/PlaylistDetail.vue`
- `src/components/playlist/UrlListManager.vue`
- `src/stores/playlists.ts`
- `src-tauri/src/commands/playlists.rs`
- `src-tauri/src/commands/url_lists.rs`
- `src-tauri/permissions/autogenerated/list_playlists.toml`
- `src-tauri/permissions/autogenerated/create_playlist.toml`
- `src-tauri/permissions/autogenerated/delete_playlist.toml`
- `src-tauri/permissions/autogenerated/add_playlist_item.toml`
- `src-tauri/permissions/autogenerated/get_playlist_items.toml`
- `src-tauri/permissions/autogenerated/list_url_lists.toml`
- `src-tauri/permissions/autogenerated/create_url_list.toml`
- `src-tauri/permissions/autogenerated/add_url_to_list.toml`
- `src-tauri/permissions/autogenerated/import_url_list.toml`

### 新規作成するファイル
- `src-tauri/src/commands/history.rs` — URL履歴の Tauri コマンド
- `src/components/common/UrlHistoryDropdown.vue` — 共通ドロップダウンコンポーネント

### 変更するファイル
- `src-tauri/src/db/schema.sql` — playlist 系テーブル削除、`url_history` テーブル追加
- `src-tauri/src/db/models.rs` — playlist 系モデル削除、`UrlHistoryEntry` 追加
- `src-tauri/src/db/queries.rs` — playlist 系クエリ削除、url_history クエリ追加
- `src-tauri/src/commands/mod.rs` — `playlists`/`url_lists` 削除、`history` 追加
- `src-tauri/src/lib.rs` — コマンド登録の更新
- `src-tauri/capabilities/default.json` — パーミッション更新
- `src/types/index.ts` — playlist 系型削除、`UrlHistoryEntry` 追加、`SidebarSection` から `'playlist'` 削除
- `src/App.vue` — playlist 関連コード削除、履歴保存トリガー追加
- `src/components/layout/AppSidebar.vue` — プレイリストセクション削除
- `src/components/layout/AppToolbar.vue` — 履歴ドロップダウン追加
- `src/components/images/ImageDownloadView.vue` — 履歴ドロップダウン追加、履歴保存トリガー追加

---

## Task 1: バックエンド — プレイリスト系コード削除

**Files:**
- Delete: `src-tauri/src/commands/playlists.rs`
- Delete: `src-tauri/src/commands/url_lists.rs`
- Modify: `src-tauri/src/commands/mod.rs`
- Modify: `src-tauri/src/lib.rs:23-74`
- Modify: `src-tauri/src/db/schema.sql:27-59`
- Modify: `src-tauri/src/db/models.rs:29-65`
- Modify: `src-tauri/src/db/queries.rs:164-248`
- Modify: `src-tauri/capabilities/default.json`
- Delete: `src-tauri/permissions/autogenerated/` 内の playlist/url_list 系 9ファイル（`start_playlist_download.toml` は `fetch_playlist_items` のパーミッションを管理しているため保持）

- [ ] **Step 1: Rust コマンドファイル削除**

`src-tauri/src/commands/playlists.rs` と `src-tauri/src/commands/url_lists.rs` を削除する。

- [ ] **Step 2: mod.rs からモジュール宣言削除**

`src-tauri/src/commands/mod.rs` から以下2行を削除:
```rust
pub mod playlists;
pub mod url_lists;
```

- [ ] **Step 3: lib.rs からコマンド登録削除**

`src-tauri/src/lib.rs` の `invoke_handler` から以下を削除:
```rust
// Playlists
commands::playlists::list_playlists,
commands::playlists::create_playlist,
commands::playlists::delete_playlist,
commands::playlists::add_playlist_item,
commands::playlists::get_playlist_items,
// URL Lists
commands::url_lists::list_url_lists,
commands::url_lists::create_url_list,
commands::url_lists::add_url_to_list,
commands::url_lists::import_url_list,
```

- [ ] **Step 4: DB スキーマからプレイリスト系テーブル削除**

`src-tauri/src/db/schema.sql` から以下4テーブルの CREATE 文を削除:
- `playlists` (lines 27-33)
- `playlist_items` (lines 35-43)
- `url_lists` (lines 45-50)
- `url_list_items` (lines 52-59)

- [ ] **Step 5: DB モデルからプレイリスト系 struct 削除**

`src-tauri/src/db/models.rs` から `Playlist`, `PlaylistItem`, `UrlList`, `UrlListItem` の4つの struct を削除 (lines 29-65)。

- [ ] **Step 6: DB クエリからプレイリスト系関数削除**

`src-tauri/src/db/queries.rs` から以下の関数をすべて削除 (lines 164-248):
- `list_playlists`, `create_playlist`, `delete_playlist`, `add_playlist_item`, `get_playlist_items`
- `list_url_lists`, `create_url_list`, `add_url_to_list`, `import_url_list_from_file`

- [ ] **Step 7: capabilities/default.json からパーミッション削除**

以下のエントリを削除:
```
"allow-add-playlist-item"
"allow-add-url-to-list"
"allow-create-playlist"
"allow-create-url-list"
"allow-delete-playlist"
"allow-get-playlist-items"
"allow-import-url-list"
"allow-list-playlists"
"allow-list-url-lists"
```

- [ ] **Step 8: autogenerated パーミッションファイル削除**

`src-tauri/permissions/autogenerated/` 内の以下9ファイルを削除（`start_playlist_download.toml` は `fetch_playlist_items` 用なので保持）:
```
list_playlists.toml, create_playlist.toml, delete_playlist.toml,
add_playlist_item.toml, get_playlist_items.toml,
list_url_lists.toml, create_url_list.toml, add_url_to_list.toml, import_url_list.toml
```

- [ ] **Step 9: cargo check で確認**

```bash
cargo check --manifest-path src-tauri/Cargo.toml
```
Expected: コンパイル成功（プレイリスト関連の未使用 import 警告が出る可能性あり）

- [ ] **Step 10: コミット**

```bash
git add -A src-tauri/src/commands/playlists.rs src-tauri/src/commands/url_lists.rs \
  src-tauri/src/commands/mod.rs src-tauri/src/lib.rs \
  src-tauri/src/db/schema.sql src-tauri/src/db/models.rs src-tauri/src/db/queries.rs \
  src-tauri/capabilities/default.json src-tauri/permissions/autogenerated/
git commit -m "refactor: remove playlist and url_list backend code"
```

---

## Task 2: バックエンド — URL履歴機能追加

**Files:**
- Modify: `src-tauri/src/db/schema.sql`
- Modify: `src-tauri/src/db/models.rs`
- Modify: `src-tauri/src/db/queries.rs`
- Create: `src-tauri/src/commands/history.rs`
- Modify: `src-tauri/src/commands/mod.rs`
- Modify: `src-tauri/src/lib.rs`
- Modify: `src-tauri/capabilities/default.json`

- [ ] **Step 1: DB スキーマに url_history テーブル追加**

`src-tauri/src/db/schema.sql` にプレイリスト系テーブルがあった場所に追加:
```sql
CREATE TABLE IF NOT EXISTS url_history (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  type       TEXT NOT NULL CHECK(type IN ('video', 'image')),
  url        TEXT NOT NULL,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  UNIQUE(type, url)
);
```

- [ ] **Step 2: DB モデルに UrlHistoryEntry 追加**

`src-tauri/src/db/models.rs` にプレイリスト系モデルがあった場所に追加:
```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UrlHistoryEntry {
    pub id: i64,
    pub url: String,
    pub created_at: String,
}
```

- [ ] **Step 3: DB クエリに url_history 関数追加**

`src-tauri/src/db/queries.rs` にプレイリスト系クエリがあった場所に追加:
```rust
// --- URL History ---

pub fn save_url_history(conn: &Connection, history_type: &str, url: &str) -> Result<()> {
    conn.execute(
        "INSERT INTO url_history (type, url, created_at) VALUES (?1, ?2, datetime('now'))
         ON CONFLICT(type, url) DO UPDATE SET created_at = datetime('now')",
        params![history_type, url],
    )?;
    conn.execute(
        "DELETE FROM url_history WHERE type = ?1 AND id NOT IN (
           SELECT id FROM url_history WHERE type = ?1 ORDER BY created_at DESC LIMIT 10
         )",
        params![history_type],
    )?;
    Ok(())
}

pub fn get_url_history(conn: &Connection, history_type: &str) -> Result<Vec<models::UrlHistoryEntry>> {
    let mut stmt = conn.prepare(
        "SELECT id, url, created_at FROM url_history WHERE type = ?1 ORDER BY created_at DESC LIMIT 10"
    )?;
    let rows = stmt.query_map(params![history_type], |row| {
        Ok(models::UrlHistoryEntry {
            id: row.get(0)?,
            url: row.get(1)?,
            created_at: row.get(2)?,
        })
    })?;
    rows.collect()
}

pub fn clear_url_history(conn: &Connection, history_type: &str) -> Result<()> {
    conn.execute("DELETE FROM url_history WHERE type = ?1", params![history_type])?;
    Ok(())
}
```

- [ ] **Step 4: Tauri コマンドファイル作成**

`src-tauri/src/commands/history.rs` を新規作成:
```rust
use tauri::State;
use crate::state::AppState;
use crate::db::{models::UrlHistoryEntry, queries};

#[tauri::command]
pub async fn save_url_history(
    history_type: String,
    url: String,
    state: State<'_, AppState>,
) -> Result<(), String> {
    let db = state.db.lock().await;
    queries::save_url_history(&db, &history_type, &url)
        .map_err(|e| format!("DB error: {}", e))
}

#[tauri::command]
pub async fn get_url_history(
    history_type: String,
    state: State<'_, AppState>,
) -> Result<Vec<UrlHistoryEntry>, String> {
    let db = state.db.lock().await;
    queries::get_url_history(&db, &history_type)
        .map_err(|e| format!("DB error: {}", e))
}

#[tauri::command]
pub async fn clear_url_history(
    history_type: String,
    state: State<'_, AppState>,
) -> Result<(), String> {
    let db = state.db.lock().await;
    queries::clear_url_history(&db, &history_type)
        .map_err(|e| format!("DB error: {}", e))
}
```

- [ ] **Step 5: mod.rs にモジュール追加**

`src-tauri/src/commands/mod.rs` に追加:
```rust
pub mod history;
```

- [ ] **Step 6: lib.rs にコマンド登録追加**

`src-tauri/src/lib.rs` の `invoke_handler` に追加（プレイリストがあった場所）:
```rust
// URL History
commands::history::save_url_history,
commands::history::get_url_history,
commands::history::clear_url_history,
```

- [ ] **Step 7: capabilities/default.json にパーミッション追加**

パーミッション配列にアルファベット順で追加:
```json
"allow-clear-url-history",
"allow-get-url-history",
"allow-save-url-history"
```

- [ ] **Step 8: cargo check で確認**

```bash
cargo check --manifest-path src-tauri/Cargo.toml
```
Expected: コンパイル成功

- [ ] **Step 9: コミット**

```bash
git add src-tauri/src/commands/history.rs src-tauri/src/commands/mod.rs \
  src-tauri/src/lib.rs src-tauri/src/db/schema.sql src-tauri/src/db/models.rs \
  src-tauri/src/db/queries.rs src-tauri/capabilities/default.json
git commit -m "feat: add url_history backend (save/get/clear commands)"
```

---

## Task 3: フロントエンド — プレイリスト系コード削除

**Files:**
- Delete: `src/components/playlist/PlaylistList.vue`
- Delete: `src/components/playlist/PlaylistDetail.vue`
- Delete: `src/components/playlist/UrlListManager.vue`
- Delete: `src/stores/playlists.ts`
- Modify: `src/types/index.ts`
- Modify: `src/components/layout/AppSidebar.vue`
- Modify: `src/App.vue`

- [ ] **Step 1: playlist コンポーネントディレクトリ削除**

`src/components/playlist/` ディレクトリごと削除。

- [ ] **Step 2: playlists ストア削除**

`src/stores/playlists.ts` を削除。

- [ ] **Step 3: types/index.ts から型定義削除・修正**

`src/types/index.ts` から以下を削除:
- `Playlist` インターフェース (lines 37-43)
- `PlaylistItem` インターフェース (lines 45-52)
- `UrlList` インターフェース (lines 54-59)
- `UrlListItem` インターフェース (lines 61-68)

`SidebarSection` 型から `'playlist'` を削除。

- [ ] **Step 4: AppSidebar.vue からプレイリストセクション削除**

`src/components/layout/AppSidebar.vue` から:
- `PlaylistList` の import を削除
- `'select-playlist'` と `'select-url-list'` の emit 定義を削除
- プレイリストセクションのテンプレート (lines 95-102) を削除

- [ ] **Step 5: App.vue からプレイリスト関連コード削除**

`src/App.vue` から以下を削除:
- `PlaylistDetail` の import (line 25)
- `selectedPlaylistId` ref (line 48)
- `selectedUrlListId` ref (line 49)
- `selectedPlaylist` computed (lines 66-70)
- `sectionLabel` の `'playlist': 'プレイリスト'` エントリ
- `handleSelectPlaylist` 関数 (line 148)
- `handleSelectUrlList` 関数 (line 152)
- `handleDownloadFromPlaylist` 関数 (line 156)
- `AppSidebar` の `@select-playlist` と `@select-url-list` イベントハンドラ
- テンプレートの `currentSection === 'playlist'` ブロック (lines 287-292)

- [ ] **Step 6: vue-tsc で確認**

```bash
npx vue-tsc --noEmit
```
Expected: 型チェック成功

- [ ] **Step 7: コミット**

```bash
git add -A src/components/playlist/ src/stores/playlists.ts \
  src/types/index.ts src/components/layout/AppSidebar.vue src/App.vue
git commit -m "refactor: remove playlist frontend code (components, store, types)"
```

---

## Task 4: フロントエンド — URL履歴コンポーネント作成

**Files:**
- Modify: `src/types/index.ts`
- Create: `src/components/common/UrlHistoryDropdown.vue`

- [ ] **Step 1: UrlHistoryEntry 型を追加**

`src/types/index.ts` に追加:
```ts
export interface UrlHistoryEntry {
  id: number
  url: string
  created_at: string
}
```

- [ ] **Step 2: UrlHistoryDropdown.vue を作成**

`src/components/common/UrlHistoryDropdown.vue` を新規作成:

```vue
<script setup lang="ts">
import { ref, onMounted, onUnmounted } from 'vue'
import { invoke } from '@tauri-apps/api/core'
import type { UrlHistoryEntry } from '../../types'

const props = defineProps<{
  type: 'video' | 'image'
}>()

const emit = defineEmits<{
  select: [url: string]
}>()

const open = ref(false)
const items = ref<UrlHistoryEntry[]>([])
const dropdownRef = ref<HTMLElement | null>(null)
const buttonRef = ref<HTMLElement | null>(null)

async function toggle() {
  if (open.value) {
    open.value = false
    return
  }
  try {
    items.value = await invoke<UrlHistoryEntry[]>('get_url_history', { historyType: props.type })
  } catch (e) {
    console.error('Failed to fetch URL history:', e)
    items.value = []
  }
  open.value = true
}

function selectItem(url: string) {
  emit('select', url)
  open.value = false
}

function handleClickOutside(e: MouseEvent) {
  if (!open.value) return
  const target = e.target as Node
  if (dropdownRef.value?.contains(target) || buttonRef.value?.contains(target)) return
  open.value = false
}

onMounted(() => document.addEventListener('click', handleClickOutside))
onUnmounted(() => document.removeEventListener('click', handleClickOutside))
</script>

<template>
  <div class="relative">
    <button
      ref="buttonRef"
      @click="toggle"
      class="w-8 h-8 flex items-center justify-center rounded-md hover:bg-neutral-100 dark:hover:bg-neutral-800 transition-colors text-neutral-500 hover:text-[var(--color-accent)]"
      title="URL履歴"
    >
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
              d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
      </svg>
    </button>

    <div
      v-if="open"
      ref="dropdownRef"
      class="absolute top-full mt-1 right-0 w-80 max-h-64 overflow-y-auto rounded-lg bg-white dark:bg-neutral-800 shadow-lg border border-[var(--color-separator)] z-50"
    >
      <div v-if="items.length === 0" class="px-3 py-4 text-center text-xs text-neutral-400">
        履歴がありません
      </div>
      <button
        v-for="item in items"
        :key="item.id"
        class="w-full text-left px-3 py-2 text-xs hover:bg-neutral-100 dark:hover:bg-neutral-700 transition-colors border-b border-[var(--color-separator)] last:border-b-0"
        @click="selectItem(item.url)"
      >
        <span class="block truncate font-mono">{{ item.url }}</span>
        <span class="block text-[10px] text-neutral-400 mt-0.5">{{ item.created_at }}</span>
      </button>
    </div>
  </div>
</template>
```

- [ ] **Step 3: vue-tsc で確認**

```bash
npx vue-tsc --noEmit
```
Expected: 型チェック成功

- [ ] **Step 4: コミット**

```bash
git add src/types/index.ts src/components/common/UrlHistoryDropdown.vue
git commit -m "feat: add UrlHistoryDropdown component"
```

---

## Task 5: フロントエンド — 履歴ドロップダウン統合と保存トリガー

**Files:**
- Modify: `src/components/layout/AppToolbar.vue`
- Modify: `src/components/images/ImageDownloadView.vue`
- Modify: `src/App.vue`

- [ ] **Step 1: AppToolbar.vue に履歴ドロップダウン追加**

`src/components/layout/AppToolbar.vue` に変更:

import 追加:
```ts
import UrlHistoryDropdown from '../common/UrlHistoryDropdown.vue'
```

テンプレートの URL input と ブラウザ取得ボタンの間（`</input>` の直後、`<!-- ブラウザからURL取得ボタン -->` の直前）に挿入:
```html
<UrlHistoryDropdown
  v-if="!isImageSection"
  type="video"
  @select="(url: string) => urlInput = url"
/>
```

注: `isImageSection` が true の場合はURL入力欄自体が disabled になるため、履歴ボタンも非表示にする。

- [ ] **Step 2: ImageDownloadView.vue に履歴ドロップダウン追加**

`src/components/images/ImageDownloadView.vue` に変更:

import 追加:
```ts
import UrlHistoryDropdown from '../common/UrlHistoryDropdown.vue'
```

テンプレートの URL input と "取得" ボタンの間に挿入:
```html
<UrlHistoryDropdown
  type="image"
  @select="(u: string) => url = u"
/>
```

- [ ] **Step 3: ImageDownloadView.vue に履歴保存トリガー追加**

`src/components/images/ImageDownloadView.vue` の `handleScrape` 関数を修正:

import 追加:
```ts
import { invoke } from '@tauri-apps/api/core'
```

`handleScrape` を修正:
```ts
async function handleScrape() {
  if (!url.value.trim()) return
  invoke('save_url_history', { historyType: 'image', url: url.value.trim() }).catch(() => {})
  await imagesStore.scrapeUrl(url.value.trim(), minWidth.value, minHeight.value)
}
```

注: `save_url_history` は fire-and-forget（失敗しても画像取得は継続）。

- [ ] **Step 4: App.vue に動画URL履歴保存トリガー追加**

`src/App.vue` の `handleStartDownload` 関数内で、ダウンロード開始の直前に保存を追加。

import に `invoke` が既にあるか確認し、なければ追加:
```ts
import { invoke } from '@tauri-apps/api/core'
```

`handleStartDownload` 内で `downloadsStore.startDownload()` 呼び出しの直前に追加:
```ts
invoke('save_url_history', { historyType: 'video', url }).catch(() => {})
```

注: `handleStartDownload(url: string, options: DownloadOptions)` の引数 `url` を使う。`downloadUrl.value` ではない。

- [ ] **Step 5: vue-tsc で確認**

```bash
npx vue-tsc --noEmit
```
Expected: 型チェック成功

- [ ] **Step 6: cargo check で最終確認**

```bash
cargo check --manifest-path src-tauri/Cargo.toml
```
Expected: コンパイル成功

- [ ] **Step 7: コミット**

```bash
git add src/components/layout/AppToolbar.vue src/components/images/ImageDownloadView.vue src/App.vue
git commit -m "feat: integrate URL history dropdown and save triggers"
```

---

## Task 6: 動作確認

- [ ] **Step 1: pnpm tauri dev で起動確認**

```bash
pnpm tauri dev
```

確認項目:
1. アプリが正常に起動する
2. サイドバーにプレイリストセクションが表示されない
3. 動画URL欄の横に時計アイコンが表示される（クリックで「履歴がありません」表示）
4. 画像URL欄の横に時計アイコンが表示される（クリックで「履歴がありません」表示）
5. 動画URLを入力してダウンロード実行 → 履歴に保存される
6. 画像URLを入力して取得実行 → 履歴に保存される
7. 履歴ドロップダウンから選択 → URL欄にセットされる（実行はされない）
8. 同じURLを再度使用 → 履歴の先頭に移動する
9. 10件超保存 → 古いものが自動削除される

- [ ] **Step 2: 最終コミット（必要に応じて修正後）**

```bash
git add -A
git commit -m "feat: url history feature complete — replaces playlist feature"
```
